import React, { useState } from 'react';
import { Home, Radio, Music, ListMusic, Crown } from 'lucide-react';
import StreamPage from './StreamPage';

export default function App() {
  const [activeTab, setActiveTab] = useState('home');

  return (
    <div className="min-h-screen bg-[#0D0D12] text-white flex flex-col justify-between max-w-md mx-auto relative pb-20 shadow-2xl font-sans">
      
      {/* ОСНОВНОЙ КОНТЕНТ */}
      <main className="flex-1 overflow-y-auto">
        {activeTab === 'home' && (
          <div className="p-4 space-y-4">
            <h1 className="text-2xl font-bold">Lumi Music</h1>
            <p className="text-xs text-gray-400">Добро пожаловать в чистую версию приложения!</p>
          </div>
        )}

        {activeTab === 'stream' && (
          <StreamPage onBack={() => setActiveTab('home')} />
        )}

        {activeTab === 'library' && (
          <div className="p-4 space-y-4">
            <h1 className="text-2xl font-bold">Библиотека</h1>
            <p className="text-xs text-gray-400">Список твоих треков</p>
          </div>
        )}

        {activeTab === 'playlist' && (
          <div className="p-4 space-y-4">
            <h1 className="text-2xl font-bold">Плейлисты</h1>
            <p className="text-xs text-gray-400">Создание и управление плейлистами</p>
          </div>
        )}

        {activeTab === 'premium' && (
          <div className="p-4 space-y-4">
            <h1 className="text-2xl font-bold">Премиум</h1>
            <p className="text-xs text-gray-400">Тарифы: $3/мес, $29/год и реклама</p>
          </div>
        )}
      </main>

      {/* НИЖНЯЯ ПАНЕЛЬ НАВИГАЦИИ (Строго 5 кнопок) */}
      <nav className="fixed bottom-0 left-0 right-0 max-w-md mx-auto bg-[#0D0D12]/95 backdrop-blur-md border-t border-gray-800 flex justify-around items-center py-3 px-2 z-40">
        <button onClick={() => setActiveTab('home')} className={`flex flex-col items-center gap-1 ${activeTab === 'home' ? 'text-pink-500' : 'text-gray-400'}`}>
          <Home className="w-5 h-5" /><span className="text-[10px]">Главная</span>
        </button>
        <button onClick={() => setActiveTab('stream')} className={`flex flex-col items-center gap-1 ${activeTab === 'stream' ? 'text-pink-500' : 'text-gray-400'}`}>
          <Radio className="w-5 h-5" /><span className="text-[10px]">Эфир</span>
        </button>
        <button onClick={() => setActiveTab('library')} className={`flex flex-col items-center gap-1 ${activeTab === 'library' ? 'text-pink-500' : 'text-gray-400'}`}>
          <Music className="w-5 h-5" /><span className="text-[10px]">Библиотека</span>
        </button>
        <button onClick={() => setActiveTab('playlist')} className={`flex flex-col items-center gap-1 ${activeTab === 'playlist' ? 'text-pink-500' : 'text-gray-400'}`}>
          <ListMusic className="w-5 h-5" /><span className="text-[10px]">Плейлист</span>
        </button>
        <button onClick={() => setActiveTab('premium')} className={`flex flex-col items-center gap-1 ${activeTab === 'premium' ? 'text-pink-500' : 'text-gray-400'}`}>
          <Crown className="w-5 h-5" /><span className="text-[10px]">Премиум</span>
        </button>
      </nav>

    </div>
  );
}
